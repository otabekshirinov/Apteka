@model Diplomka.Models.Order
@{
    ViewBag.Title = "Удаление заказа";
}

<div>
    <dl class="dl-horizontal">
        <dt>Вы уверены, что хотите удалить заказ №@Model.OrderID ?</dt>
    </dl>

    <div>
        <form asp-controller="Order" asp-action="Delete" method="post">
            <div class="form-group">
                <input type="submit" class="btn btn-primary" value="Удалить" />
            </div>
        </form>
    </div>
</div>
<a asp-action="BackToHome" class="btn btn-primary"> Отмена</a>

