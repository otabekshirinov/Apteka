@model Diplomka.Models.Warehouse
@{
    ViewBag.Title = "Удаление данных о складе";
}

<div>
    <dl class="dl-horizontal">
        <dt>Вы уверены, что хотите удалить данные о складе №@Model.WarehouseID ?</dt>
    </dl>

    <div>
        <form asp-controller="Warehouse" asp-action="Delete" method="post">
            <div class="form-group">
                <input type="submit" class="btn btn-primary" value="Удалить" />
            </div>
        </form>
    </div>
</div>
<a asp-action="BackToHome" class="btn btn-primary"> Отмена</a>

